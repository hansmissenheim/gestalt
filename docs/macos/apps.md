# Applications

## Homebrew

Install Homebrew, then use it for all CLI tools and apps.

```bash
```

### Formulae

CLI tools installed via `brew install`.

### Casks

GUI apps installed via `brew install --cask`.

## Mac App Store

Apps installed via `mas` or directly from the App Store.

## Manual Installs

Apps that can't be installed through a package manager.
