# SSH

## OpenSSH Server

Enable and configure the built-in Windows OpenSSH server.

### Install

Enable via Optional Features.

### sshd_config

Key settings applied to `C:\ProgramData\ssh\sshd_config`.

### Service

Set the service to start automatically.

## Authorized Keys

Where to place the `authorized_keys` file and any permission notes.

## Firewall

Any firewall rules needed to allow inbound SSH connections.
